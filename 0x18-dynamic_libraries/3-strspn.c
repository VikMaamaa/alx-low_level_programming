#include "main.h"

/**
 * _strspn - function that gets the length of a prefix substring
 *
 * @s: the string, first param
 * @accept: the characters that will be counted in the initial segment of s
 *
 * Return: number of bytes in the iniial segment of s which consist of
 * only bytes from accept
 */
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
